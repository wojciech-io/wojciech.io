# Build-time / dev-time tooling — not deployed, no untrusted input.
# RegExp built from glossary terms and JSX tag names (all author-controlled
# constants), so the detect-non-literal-regexp rule triggers false positives.
scripts/i18n/

# Test fixtures
tests/
