# Codex tasks

Closed task specs the Tech Lead writes for Codex. One file per task: `<ISO8601>-<slug>.md` with frontmatter (task, branch_hint, acceptance) and a body. Codex writes a `*-result.md` companion when done. Format and rules: `docs/codex-handoff-protocol.md`.
