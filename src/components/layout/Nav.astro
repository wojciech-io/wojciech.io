---
interface Props { prefix?: string; }
const { prefix = '' } = Astro.props;

export const links = [
  { label: 'Work',     href: '/work',       pl: 'Projekty',  it: 'Progetti'  },
  { label: 'Apps',     href: '/apps',       pl: 'Aplikacje', it: 'App'       },
  { label: 'AI',       href: '/ai-systems', pl: 'AI',        it: 'AI'        },
  { label: 'Insights', href: '/insights',   pl: 'Artykuly',  it: 'Articoli'  },
  { label: 'About',    href: '/about',      pl: 'O mnie',    it: 'Chi sono'  },
  { label: 'Contact',  href: '/contact',    pl: 'Kontakt',   it: 'Contatti'  },
];

const current = Astro.url.pathname;
---

<nav aria-label="Primary navigation">
  <ul class="flex items-center gap-6 list-none m-0 p-0">
    {links.map(({ label, href, external, pl, it }) => {
      const dest = external ? href : prefix + href;
      const isActive = !external && (href === '/' ? current === dest + '/' || current === dest : current.startsWith(dest));
      return (
        <li>
          <a
            href={dest}
            data-en={label}
            data-pl={pl}
            data-it={it}
            aria-current={isActive ? 'page' : undefined}
            class:list={[
              'nav-link text-sm font-medium tracking-wide transition-colors duration-150 pb-0.5',
              isActive
                ? 'text-[var(--color-text)]'
                : 'text-[var(--color-text-muted)] hover:text-[var(--color-text)]',
            ]}
            {...(external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
          >
            {label}
          </a>
        </li>
      );
    })}
  </ul>
</nav>
