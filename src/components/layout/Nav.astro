---
import { getPrimaryNavLinks, getLocaleFromPath } from '../../data/uiStrings';

const current = Astro.url.pathname;
const links = getPrimaryNavLinks(current);
const isAr = getLocaleFromPath(current) === 'ar';
---

<nav aria-label={isAr ? 'التنقّل الرئيسي' : 'Primary navigation'}>
  <ul class="flex items-center gap-6 list-none m-0 p-0">
    {links.map(({ label, href, minWidth }) => {
      const isActive = href === '/' ? current === href : current.startsWith(href);
      return (
        <li>
          <a
            href={href}
            aria-current={isActive ? 'page' : undefined}
            style={`min-width:${minWidth};text-align:center`}
            class:list={[
              'nav-link inline-block obs-mono text-xs font-medium tracking-wide transition-colors duration-150 pb-0.5',
              isActive
                ? 'text-[var(--color-text)]'
                : 'text-[var(--color-text-muted)] hover:text-[var(--color-text)]',
            ]}
          >
            {label}
          </a>
        </li>
      );
    })}
  </ul>
</nav>
