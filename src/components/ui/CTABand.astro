---
interface Props {
  heading?: string;
  subtext?: string;
  cta?: string;
  ctaHref?: string;
  ctaSecondary?: string;
  ctaSecondaryHref?: string;
}
const {
  heading = 'Have a system worth fixing?',
  subtext = 'Send the context, the current bottleneck, and what already exists. I will tell you where I would start.',
  cta = 'Book a call',
  ctaHref = '/contact#book-call',
  ctaSecondary = 'See the work',
  ctaSecondaryHref = '/work',
} = Astro.props;
const ctaIsExternal = ctaHref.startsWith('http');
const secondaryIsExternal = ctaSecondaryHref.startsWith('http');
const headingLines = heading.split('\n');
---

<section class="py-14 md:py-20 relative overflow-hidden bg-[var(--color-surface)]">
  <div class="pointer-events-none absolute inset-0" style="background: radial-gradient(ellipse at 20% 50%, rgba(235,255,0,0.04) 0%, transparent 60%);" aria-hidden="true"></div>
  <div class="pointer-events-none absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-[var(--color-accent)]/40 to-transparent" aria-hidden="true"></div>

  <div class="relative mx-auto grid max-w-6xl gap-6 px-6 md:grid-cols-[1fr_auto] md:items-end">
    <div class="max-w-2xl">
      <p class="mb-3 text-xs font-semibold uppercase tracking-widest text-[var(--color-text-dim)]">Next step</p>
      <h2 class="text-2xl font-semibold leading-[1.12] tracking-tight text-[var(--color-text)] md:text-3xl">
        {headingLines.map((line, index) => (
          <>
            <span class={index > 0 ? 'text-[var(--color-text-muted)]' : ''}>{line}</span>
            {index < headingLines.length - 1 && <br />}
          </>
        ))}
      </h2>

      <p class="mt-3 max-w-xl text-sm leading-relaxed text-[var(--color-text-muted)]">
        {subtext}
      </p>
    </div>

    <div class="flex flex-wrap gap-3 md:justify-end">
      <a
        href={ctaHref}
        {...(ctaIsExternal ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
        class="btn-press inline-flex items-center justify-center gap-2 rounded-[var(--radius-btn)] bg-[var(--color-accent-cta)] px-5 py-2.5 text-sm font-semibold text-[var(--color-accent-text)] transition-colors hover:opacity-90"
        data-mp-cta={cta}
        data-mp-location="cta_band"
      >
        <span>{cta}</span>
        <svg width="13" height="13" viewBox="0 0 13 13" fill="none" aria-hidden="true">
          <path d="M2 6.5h9M7 2.5l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
      <a
        href={ctaSecondaryHref}
        {...(secondaryIsExternal ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
        class="btn-press inline-flex items-center justify-center gap-2 rounded-[var(--radius-btn)] border border-[var(--color-border-2)] px-5 py-2.5 text-sm font-medium text-[var(--color-text-muted)] transition-colors hover:border-[var(--color-border)] hover:text-[var(--color-text)]"
      >
        {ctaSecondary}
      </a>
    </div>
  </div>
</section>
