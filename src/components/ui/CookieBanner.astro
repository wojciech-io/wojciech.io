---
// Analytics consent banner
// - Compact single-line design, slides up from the bottom
// - Stores choice in localStorage as 'cookie-consent' = 'accepted' | 'declined'
// - On accept: fires initAfterConsent() which initialises GA4 + Mixpanel
// - On decline: fires optOut() so Mixpanel stays in opt-out mode
---

<div
  id="cookie-banner"
  class="fixed bottom-0 left-0 right-0 z-50 translate-y-full transition-transform duration-300 ease-out"
  role="dialog"
  aria-label="Analytics consent"
  aria-live="polite"
>
  <div class="border-t border-[var(--color-border)] bg-[var(--color-surface)]/95 backdrop-blur-md">
    <div class="mx-auto flex max-w-6xl flex-col gap-3 px-6 py-3 sm:flex-row sm:items-center sm:gap-6">

      <!-- Text -->
      <p class="flex-1 text-xs text-[var(--color-text-muted)] leading-relaxed">
        I use <strong class="text-[var(--color-text)] font-medium">Mixpanel</strong> and <strong class="text-[var(--color-text)] font-medium">Google Analytics</strong> to understand which content is useful: anonymously, with EU data residency, no advertising.
        <a href="/privacy/" class="ml-1 text-[var(--color-accent)] hover:text-[var(--color-accent-hi)] underline underline-offset-2 whitespace-nowrap">Full details →</a>
      </p>

      <!-- Actions -->
      <div class="flex shrink-0 items-center gap-2">
        <button
          id="cookie-decline"
          class="text-xs text-[var(--color-text-dim)] hover:text-[var(--color-text)] transition-colors px-3 py-1.5 rounded-[var(--radius-btn)] hover:bg-[var(--color-surface-2)]"
        >
          No thanks
        </button>
        <button
          id="cookie-accept"
          class="btn-press text-xs font-semibold px-4 py-1.5 rounded-[var(--radius-btn)] bg-[var(--color-accent-cta)] text-[var(--color-accent-text)] hover:opacity-90 transition-opacity"
        >
          Accept analytics
        </button>
      </div>

    </div>
  </div>
</div>

<script>
  import { initAfterConsent, optOut } from '../../utils/mixpanel';

  const COOKIE_KEY = 'cookie-consent';
  const banner = document.getElementById('cookie-banner') as HTMLElement;
  const acceptBtn = document.getElementById('cookie-accept') as HTMLButtonElement;
  const declineBtn = document.getElementById('cookie-decline') as HTMLButtonElement;

  function hideBanner() {
    banner.classList.add('translate-y-full');
    banner.classList.remove('translate-y-0');
  }

  function showBanner() {
    // Small delay so it slides in after page has settled
    setTimeout(() => {
      banner.classList.remove('translate-y-full');
      banner.classList.add('translate-y-0');
    }, 1000);
  }

  const consent = localStorage.getItem(COOKIE_KEY);

  if (consent === 'accepted') {
    initAfterConsent();
  } else if (!consent) {
    showBanner();
  }

  acceptBtn?.addEventListener('click', () => {
    localStorage.setItem(COOKIE_KEY, 'accepted');
    hideBanner();
    initAfterConsent();
  });

  declineBtn?.addEventListener('click', () => {
    localStorage.setItem(COOKIE_KEY, 'declined');
    hideBanner();
    optOut();
  });
</script>
