# Open threads

> One section per thread. Close threads by removing the section.
> Thread ID format: `<topic-slug>` (kebab-case, stable across messages).

## sprint-1-baseline-flake-watch

Watching for flaky tests in the first 2 weeks. Threshold: any test that fails 2+ times in a row without source change → quarantine + investigate.

## a11y-baseline-pass

Goal: zero `serious` or `critical` axe violations across smoke pages by end of Sprint 1. Anything found → file Issue with `agent:design` ownership.
