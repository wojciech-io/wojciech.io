{
  "name": "wojciech.io",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/generate-shipped.mjs",
    "build": "astro build",
    "postbuild": "pagefind --site dist --output-path dist/search",
    "build:site": "astro build",
    "build:app": "npm run build --workspace=apps/app",
    "build:subscribe": "npm run build --workspace=apps/subscribe",
    "build:notch": "npm run build --workspace=apps/notch",
    "build:growthhub": "npm run build --workspace=apps/growthhub",
    "build:academy": "npm run build --workspace=apps/academy",
    "build:dev": "npm run build --workspace=apps/dev",
    "deploy:dev": "npm run build:dev && cd apps/dev && npx wrangler pages deploy dist --project-name=wojciech-io-dev --branch=main --commit-dirty=true --commit-message=dev-dashboard-deploy",
    "deploy:subscribe": "npm run build:subscribe && cd apps/subscribe && npx wrangler pages deploy dist --project-name=subscribe-wojciech-io --branch=main --commit-dirty=true --commit-message=subscribe-deploy",
    "deploy:notch": "npm run build:notch && cd apps/notch && npx wrangler pages deploy dist --project-name=notch-wojciech-io --branch=main --commit-dirty=true --commit-message=notch-deploy",
    "deploy:growthhub": "npm run build:growthhub && cd apps/growthhub && npx wrangler pages deploy dist --project-name=gh-wojciech-io --branch=main --commit-dirty=true --commit-message=growthhub-deploy",
    "deploy:academy": "npm run build:academy && cd apps/academy && npx wrangler pages deploy dist --project-name=academy-v2-wojciech-io --branch=main --commit-dirty=true --commit-message=academy-v2-deploy",
    "deploy:academy:prod": "npm run build:academy && cd apps/academy && npx wrangler pages deploy dist --project-name=akademia-wojciech-io --branch=main --commit-dirty=true --commit-message=academy-prod-cutover",
    "preview": "astro preview",
    "astro": "astro",
    "test": "playwright test",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:unit:coverage": "vitest run --coverage",
    "test:smoke": "playwright test smoke",
    "test:a11y": "playwright test a11y",
    "test:ui": "playwright test --ui",
    "test:update": "playwright test --update-snapshots",
    "test:prod": "BASE_URL=https://wojciech.io playwright test smoke"
  },
  "dependencies": {
    "@astrojs/mdx": "^6.0.2",
    "@astrojs/sitemap": "^3.7.2",
    "@resvg/resvg-js": "^2.6.2",
    "@sentry/astro": "^10.56.0",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.1",
    "geist": "^1.7.0",
    "lenis": "^1.3.23",
    "mixpanel-browser": "^2.80.0",
    "motion": "^12.40.0",
    "satori": "^0.26.0",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@cloudflare/workers-types": "^4.20260602.1",
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "@pagefind/default-ui": "^1.5.2",
    "@playwright/test": "^1.49.0",
    "@types/js-yaml": "^4.0.9",
    "@types/mixpanel-browser": "^2.66.0",
    "@vitest/coverage-v8": "^4.1.7",
    "pagefind": "^1.5.2",
    "vitest": "^4.1.7"
  },
  "overrides": {
    "postcss": ">=8.5.10"
  }
}
