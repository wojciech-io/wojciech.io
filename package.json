{
  "name": "wojciech.io",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:site": "astro build",
    "build:app": "npm run build --workspace=apps/app",
    "build:subscribe": "npm run build --workspace=apps/subscribe",
    "build:notch": "npm run build --workspace=apps/notch",
    "build:growthhub": "npm run build --workspace=apps/growthhub",
    "deploy:subscribe": "npm run build:subscribe && cd apps/subscribe && npx wrangler pages deploy dist --project-name=subscribe-wojciech-io --branch=main --commit-dirty=true",
    "deploy:notch": "npm run build:notch && cd apps/notch && npx wrangler pages deploy dist --project-name=notch-wojciech-io --branch=main --commit-dirty=true",
    "deploy:growthhub": "npm run build:growthhub && cd apps/growthhub && npx wrangler pages deploy dist --project-name=gh-wojciech-io --branch=main --commit-dirty=true",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.4",
    "@astrojs/sitemap": "^3.7.2",
    "@resvg/resvg-js": "^2.6.2",
    "@sentry/astro": "^10.53.1",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.1",
    "geist": "^1.7.0",
    "satori": "^0.26.0",
    "tailwindcss": "^4.3.0"
  }
}
